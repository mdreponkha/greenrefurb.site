rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for site content, quotes, and admin updates
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
